# Applied Rust Engineering Standard

This project follows the supplied Rust Engineering Best Practices document.
The supplied document was reviewed during project design. Applied rules
include typed domain values, fixed-width FFI, explicit bounds, typed capacity
errors, scalar reference paths, caller-owned buffers, no hidden allocation, and
allocation-free steady state.
