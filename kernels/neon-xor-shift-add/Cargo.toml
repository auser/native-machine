[package]
name = "neon-xor-shift-add"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[lib]
crate-type = ["cdylib", "rlib"]

[dev-dependencies]
reference-xor-shift-add = { path = "../reference-xor-shift-add" }

[profile.release]
panic = "abort"
