[package]
name = "avx2-fma-matmul"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[lib]
crate-type = ["cdylib", "rlib"]

[dev-dependencies]
reference-matmul = { path = "../reference-matmul" }

[profile.release]
panic = "abort"
